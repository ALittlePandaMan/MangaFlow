# 安全策略

## 支持范围

当前项目仍处于早期开发阶段，只维护 `main` 分支的最新版本。修复会优先合入 `main`，暂不承诺旧提交的安全更新。

## 报告漏洞

请使用仓库的 [GitHub 私密安全公告](https://github.com/ALittlePandaMan/MangaFlow/security/advisories/new) 报告漏洞。不要在公开 issue 中提交 API Key、访问令牌、私有服务地址、完整 `.env`、`config.yaml` 或未授权公开的漫画内容。

报告中请包含影响范围、复现步骤、受影响版本和可行的缓解方式。维护者确认前，请不要公开漏洞细节。

## 部署边界

MangaFlow 当前定位为可信本机或受控局域网工作台，API 尚未提供用户登录、权限隔离或公网级限流。不要把 8000/8080 端口直接暴露到互联网。需要远程访问时，请在前方部署经过配置的身份认证、TLS 反向代理、防火墙和请求限流。

云端翻译密钥只应写入被 Git 忽略的 `.env`。`config.yaml` 可以携带 Provider 地址和模型选择，但不应包含密钥。迁移或分享工程包前仍应检查其中是否包含私有端点和受版权保护的数据。
